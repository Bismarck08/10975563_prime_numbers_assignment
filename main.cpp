#include <iostream>

using namespace std;

bool isPrime(int num)
{
    for int(i=2; i<num;i++)
    {
        if (num%i==0)
        {
            return false;
        }
        return true;
int main()
{
    int sum=0;
    int num;
    cout<<"Input your prime number"<<endl;
    cin>>num;
    for (int i=1; i<num;i++
         {
             if(isPrime(1))
             {
                 sum+=1;
             }
         }
     cout<<"the sum of the numbers is"<<num<<endl;
    return 0;
}
